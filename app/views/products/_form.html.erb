<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  Product Title : <%= form.text_field :title, required: true %>
  Product Description : <%= form.text_area :description, required: true %>
  Product Price : <%= form.number_field :price, required: true %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
