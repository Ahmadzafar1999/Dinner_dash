<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <h3 class = 'navbar-brand' style="color: white"><strong> Dinner Dash </strong> </h3>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
          <%= button_to "Sign Out" , destroy_user_session_path, :method=> :delete, class: 'nav_item btn btn-outline-info' %>
          </li>
        <% else %>
          <li class="nav-item">
          <%= button_to "Sign up" , new_user_registration_path, method: :get, class: 'nav_item btn btn-outline-info' %>
          </li>
          <li class="nav-item">
          <%= button_to "Sign in" , new_user_session_path, method: :get, class: 'nav_item btn btn-outline-info' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
